[project]
name = "agentic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "m-t-chang", email = "michaeltgchang@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "celery[redis,sqlalchemy]>=5.5.3",
    "flask>=3.1.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
